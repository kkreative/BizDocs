.wrapper
  %h1= @doc.title
  %h4= @doc.user_id
  %p= @doc.content

  = link_to "All Docs", docs_path
  = link_to "Fix Doc", edit_doc_path(@doc)
  = link_to "Trash", doc_path(@doc), method: :delete, data: { confirm: "Are you sure?" }